# OpenRamble 0.27.5

Opening Dictation History no longer makes the Settings sidebar disappear.
The history stays inside the window, with its controls visible and its text
below the title bar. Switching sections and resizing the window work with
both an empty history and a long list of dictations.
